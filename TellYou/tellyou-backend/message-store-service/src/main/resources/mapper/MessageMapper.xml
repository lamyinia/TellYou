<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.com.store.infrastructure.persistence.mapper.MessageMapper">

    <insert id="insert" parameterType="org.com.store.infrastructure.persistence.po.MessageDO">
        INSERT INTO message
        (msg_id, session_id, sender_id, partition_id, seq, msg_type, appearance, content, created_at)
        VALUES
        (#{msgId}, #{sessionId}, #{senderId}, #{partitionId}, #{seq}, #{msgType}, #{appearance}, #{content}, #{createdAt})
    </insert>

</mapper>
