<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.com.store.infrastructure.persistence.mapper.UserMessageIndexMapper">

    <insert id="batchInsertIgnore" parameterType="java.util.List">
        INSERT IGNORE INTO user_message_index
        (user_id, session_id, msg_id, seq, read_state)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.sessionId}, #{item.msgId}, #{item.seq}, #{item.readState})
        </foreach>
    </insert>

</mapper>
